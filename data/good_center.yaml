get_banner:
  # 接口的基本信息
  - request_info:
      allure-title: banner接口
      url: /banners/
      method: GET
      headers:
        Content-Type: application/json
    case_info:
      # 接口请求参数,params , json ,data
      - params:
        validate:
          - length: [ '$..id', 10 ]



